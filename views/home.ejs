<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GPS by Rayyan</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            box-sizing: border-box;
            background-color: #373A40;
        }
        #map {
            height: 100vh;
            width: 100%;
        }
        .btn-location {
            z-index: 1000;
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            padding: 10px 20px;
            font-size: 1.2em;
        }
        .btn-location1{
            z-index: 1000;
            position: absolute;
            bottom: 20px;
            left: 20%;
            transform: translateX(-50%);
            padding: 10px 20px;
            font-size: 1.2em;
        }
        .btn-location2{
            z-index: 1000;
            position: absolute;
            bottom: 20px;
            left: 80%;
            transform: translateX(-50%);
            padding: 10px 20px;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
    <div id="map">
        <% if(admin){ %>
            <button id="showGeofenceBtn" class="btn btn-primary btn-location1">Show fence</button>
        <button id="stop" class="btn btn-primary btn-location2">Stop</button>
        <% }else{ %>
        <button id="btn" class="btn btn-primary btn-location">Find My Location</button>
   <% } %>
    </div>
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
