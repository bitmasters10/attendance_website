<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Form - Bootstrap Centered</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-gray-100">
    <div class="container-fluid d-flex justify-content-center align-items-center vh-100">
        <div class="bg-white rounded-lg shadow-md p-5" style="max-width: 400px;">
            <h1 class="text-3xl font-bold mb-5">Update Form</h1>
            <form action="http://localhost:3000/admin/users/<%= users.id %>?_method=PATCH" method="post">
                <div class="mb-3">
                    <label for="username" class="form-label">Enter the Username:</label>
                    <input type="text" id="username" value="<%= users.username %>" name="username" class="form-control">
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Enter the Email:</label>
                    <input type="text" id="email" name="email" value="<%= users.email %>" class="form-control">
                </div>
                <button type="submit" class="btn btn-primary btn-block">Submit</button>
            </form>
        </div>
    </div>
</body>
</html>
