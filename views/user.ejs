<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container">
        <h1 class="my-4 text-center">Users</h1>
        <% for(user of users){ %>
        <div class="card mb-3">
            <div class="card-body">
                <h2 class="card-title">ID: <%= user.id %> </h2>
                <h3 class="card-subtitle mb-2 text-muted">NAME: <%= user.username %> </h3>
                <h4 class="card-text">EMAIL: <%= user.email %> </h4>
                <a href="/admin/users/<%=user.id%>" class="btn btn-primary">Update</a>
                <form action="http://localhost:3000/admin/users/<%=user.id%>?_method=DELETE" method="post" style="display: inline;">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        <% } %>
    </div>
</body>
</html>
